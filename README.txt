This is a parser that reads JSON dumps from facebook.com/dyi.
It initially started as a personal thing, but why not publish it.

It should successfully parse any dump created since 2020.
However, to note, some fields might have moved/renamed. They will be still parsed, but not renamed "back".
